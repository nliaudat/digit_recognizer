# Build
### windaube
docker-compose build
#or linux (always docker(space)compose
docker compose build

# Run
docker-compose up -d

# Test
docker-compose exec digit-recognizer python3 -c "import tensorflow as tf; import torch; print('TF GPU:', len(tf.config.list_physical_devices('GPU')) > 0); print('PyTorch CUDA:', torch.cuda.is_available())"

/root/digit_recognizer/docker/
#docker-compose run digit-recognizer bash